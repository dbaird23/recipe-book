{
  "name": "recipe-book",
  "private": true,
  "version": "0.1.0",
  "description": "A private, invite-only recipe book for family and friends",
  "workspaces": [
    "worker",
    "web"
  ],
  "scripts": {
    "dev": "mkdir -p web/dist && concurrently -n api,web -c green,blue \"npm run dev -w worker\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "setup": "npm run setup -w worker",
    "seed": "npm run seed -w worker",
    "deploy": "npm run build && npm run deploy -w worker",
    "tail": "npm run tail -w worker"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
