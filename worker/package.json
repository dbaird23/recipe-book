{
  "name": "worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "setup": "node scripts/setup.mjs",
    "migrate": "wrangler d1 migrations apply recipe-book --remote",
    "migrate:local": "wrangler d1 migrations apply recipe-book --local",
    "seed": "node scripts/seed.mjs",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
